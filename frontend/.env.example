# Copy to `.env.local` (git-ignored) for local development.
#
# Base URL of the backend API, WITHOUT the /api/v1 suffix — src/services/api.ts
# appends it. Defaults to http://localhost:5117 when unset.
VITE_API_URL=http://localhost:5117
